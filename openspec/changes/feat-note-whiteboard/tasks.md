# Tasks: Note Whiteboard Surface

- [x] Migration: `content_kind`, `surface_data` on `notes`
- [x] Core + IPC types and `update_note` patch fields
- [x] Frontend types and library convert / surface save paths
- [x] Whiteboard host, adapter registry, shell adapter
- [x] NoteEditor + command palette convert UI (one-way, danger confirm)
- [x] List cue + docs (DATA_MODEL, API)
- [x] Unit tests for surface document helpers and core update path
