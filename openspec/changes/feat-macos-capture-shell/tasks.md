# Tasks

- [x] Add a system tray with the app icon and a capture menu item
- [x] Register a global shortcut to invoke the capture panel
- [x] Create a hidden capture window, shown and focused on demand
- [x] Wire typed IPC commands for note CRUD and search
- [x] Manage app lifecycle so the process stays alive from the tray
- [x] Connect the capture window to the core store through IPC
