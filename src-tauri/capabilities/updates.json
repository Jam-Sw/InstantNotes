{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updates",
  "description": "Library window only: check, download, and install app updates, then relaunch.",
  "windows": ["library"],
  "permissions": [
    "updater:default",
    "process:allow-restart"
  ]
}
